请按“最小必要增补”的原则补实验，不要大改框架，不要再泛化扩题。目标是让论文口径更稳，而不是再堆图。

[Task 1] 术语与实现口径统一
1. 将 utility 中的 `cost` 全部重命名为 `step_cost`。
2. 在 README / paper notes 中明确：
   - `step_cost` 是 policy 内部的归一化步代价
   - `token_usage` 和 `wall_time` 是评测指标，不是当前决策输入
3. 检查代码、图标题、表头、导出 csv 字段名是否统一，避免 method 写 cost、实验写 tokens/time，造成歧义。

[Task 2] 补一个“真实成本版本”的轻量对照实验
目标：验证当前 step_cost proxy 是否至少与真实成本趋势一致。
做两个轻量变体即可，不需要全量重跑所有实验：
1. token-cost utility:
   U = gain - l1 * normalized_token_cost - l2 * uncertainty - l3 * redundancy
2. latency-cost utility:
   U = gain - l1 * normalized_latency_cost - l2 * uncertainty - l3 * redundancy
要求：
- 只在主 setting 跑 policy、threshold、react、workflow 四类即可
- 输出一张小表：proxy(step_cost) / token_cost / latency_cost 三种版本的 F1、tokens、wall_time
目的不是追求最优，而是说明当前 proxy 设计是否合理

[Task 3] 强化 redundancy 定义并做小型消融
当前 redundancy 太弱，只检测 exact query match。
请升级到一个更合理但实现简单的版本，优先级：
1. query embedding cosine similarity
如果太麻烦，退而求其次：
2. retrieved passage overlap
要求：
- 增加 `redundancy_mode = exact / semantic`
- 跑一个小型对照：full policy(exact) vs full policy(semantic)
- 输出：
  - avg_f1
  - avg_tokens
  - avg_wall_time
  - redundant_tool_calls
目的：证明 redundancy 不是“摆设项”

[Task 4] 补强 workflow baseline
当前 workflow baseline 太弱，请至少增加两个 fixed workflow baseline：
1. workflow-search-twice:
   fixed search twice, then answer
2. workflow-search-verify:
   search once, verify once, then answer
要求：
- 明确它们是 fixed pipeline baselines
- 不要引入过多复杂逻辑
- 与现有 minimal workflow 一起列在主表里
目的：避免 reviewer 认为 workflow baseline 不公平

[Task 5] expected_gain / uncertainty 的口径补充
不要把它们包装成严格概率。
请在 README / paper note 中补充一段标准表述：
- expected_gain: LLM self-estimated marginal value of one more retrieval step
- uncertainty: LLM self-estimated uncertainty of current answer
- both are heuristic signals clipped to [0,1], not calibrated probabilities
同时导出一个小分析表：
- expected_gain bucket vs continue rate
- uncertainty bucket vs continue rate
如果容易做，再加与最终 F1 的粗相关性
目的：给论文一个可辩护的解释

[Task 6] 输出最终论文用表
请整理出以下最终表格：
1. 主结果表：
   Method | F1 | Tokens | Wall Time | Efficiency
2. 成本定义对照表：
   step_cost vs token_cost vs latency_cost
3. Workflow fairness 表：
   minimal workflow / workflow-search-twice / workflow-search-verify
4. Ablation 表：
   full / -expected_gain / -uncertainty / -redundancy / -stop
5. Heuristic signal analysis 表（若完成）

[Task 7] 不再扩的内容
以下先不要做：
- 不做 RL 训练
- 不做第二大 benchmark，除非前面都补完且时间充足
- 不做更复杂 planner / beam search / global optimization
- 不重构为 learnable policy network
目标是把现有 paper 打磨到“定义清楚、实验公平、结论可 defend”