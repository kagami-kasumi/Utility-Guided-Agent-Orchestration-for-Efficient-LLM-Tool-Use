317 定义为 final defense experiments，不再扩题。只补 316 中待补齐的 5 项：policy(token_cost)、policy(latency_cost)、policy(semantic redundancy)、workflow-search-twice、workflow-search-verify。目标不是再追求更高分，而是验证 step_cost proxy、redundancy 设计和 workflow fairness 是否可辩护。跑完后直接汇总成最终主表、workflow fairness 表、cost definition 表，并停止新增实验。